# border-radius-generator

Set all 8 border-radius values independently and copy the CSS.

Most border-radius tools only let you set one value. This one exposes all four corners, each with a horizontal and vertical radius, for full control over blob/organic shapes.

## Run

```bash
npm install && npm run dev
```
